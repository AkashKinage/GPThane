<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gpthane">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">
        <activity android:name=".Student.StudentNotice"></activity>
        <activity android:name=".Teacher.TeacherUploadNotice" />
        <activity android:name=".Student.StudentActivity" />
        <activity android:name=".Student.StudentLogin" />
        <activity android:name=".Student.StudentRegister" />
        <activity android:name=".Teacher.TeacherActivity" />
        <activity android:name=".Teacher.TeacherLogin" />
        <activity android:name=".Teacher.TeacherRegister" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>